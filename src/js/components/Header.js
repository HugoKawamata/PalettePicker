import React from "react";

export default class Header extends React.Component {
    render() {
        return (
            <header>
                <h1>Palette Picker</h1>
                <h4>Plan your colour scheme and export it in scss</h4>
            </header>
        );
    }
}